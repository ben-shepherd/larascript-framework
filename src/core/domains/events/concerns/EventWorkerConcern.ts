import { ICtor } from "@src/core/interfaces/ICtor";

const EventWorkerConcern = (Base: ICtor) => {
    return class EventWorkerConcern extends Base {

        // todo read and process events from db
    
    }
}

export default EventWorkerConcern