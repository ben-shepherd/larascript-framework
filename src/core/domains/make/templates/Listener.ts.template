import EventListener from "@src/core/domains/events/services/EventListener";
import { Payload } from "@src/app/events/subscribers/#name#Subscriber";

export class #name#Listener extends EventListener<Payload> {
    
    handle = async (payload: Payload) => {
        console.log('[#name#Listener]')
    }
}