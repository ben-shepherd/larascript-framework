services:
  api:
    build:
      context: ../..
      dockerfile: docker/dev/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../../:/app
      - /app/node_modules
    working_dir: /app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
